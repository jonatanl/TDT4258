#include "logic.h"

// Function prototypes
void detect_collisions(gamestate* game);

struct gamestate* game;

void detect_collisions(gamestate* gamestate) {
	int n_asteroids = gamestate->n_asteroids;

	for (int i = 0; i < n_asteroids; ++i) {
		
	}
}
